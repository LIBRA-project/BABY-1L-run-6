name: baby-1l-run-6
channels:
  - conda-forge
dependencies:
  - python
  - openmc # add version tag
  - openmc-plotter
  - numpy # add version tag
  - scipy # add version tag
  - matplotlib # add version tag
  - sympy
  - pandas
  - pint-pandas
  - nbconvert
  - ipykernel
  - pint
  - papermill
  - pip
  - pip:
      - git+https://github.com/libra-project/libra-toolbox@v0.8
      - openmc_data_downloader
      - matplotlib-label-lines
      - h-transport-materials
